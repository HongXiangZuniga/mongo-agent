## 1. Dominio (pkg/agent/agent.go)

- [x] 1.1 Crear el archivo `pkg/agent/agent.go` con `package agent`. Definir el tipo `type Role string` y las constantes `RoleUser Role = "user"`, `RoleAssistant Role = "assistant"`, `RoleTool Role = "tool"`, `RoleSystem Role = "system"`. Criterio de listo: el archivo compila de forma aislada (`gofmt -l pkg/agent/agent.go` no reporta errores de formato).
- [x] 1.2 En el mismo archivo, definir `type ToolCall struct { ID string; Name string; Arguments string }` donde `Arguments` es el JSON crudo de los argumentos de la tool call (sin parsear). Criterio de listo: struct exportado, campos exportados, sin lógica.
- [x] 1.3 En el mismo archivo, definir `type Message struct { Role Role; Content string; ToolCalls []ToolCall; ToolCallID string; CreatedAt time.Time }`. `ToolCallID` solo se usa cuando `Role == RoleTool` (identifica a qué `ToolCall.ID` responde). Importar `time` de la librería estándar. Criterio de listo: struct compila, único import es `time`.
- [x] 1.4 En el mismo archivo, definir `type Question struct { SessionID string; Text string }`. Criterio de listo: struct exportado con esos dos campos exactos.
- [x] 1.5 En el mismo archivo, definir `type Answer struct { SessionID string; Text string; ToolCallsExecuted int }`. Criterio de listo: struct exportado con esos tres campos exactos.
- [x] 1.6 Verificar que `pkg/agent/agent.go` NO importa ningún paquete externo (`gin`, `mongo-driver`, `redis`, `net/http`) — solo `time` de librería estándar. Criterio de listo: `grep -E '"(gin|mongo-driver|redis|net/http)"' pkg/agent/agent.go` no devuelve resultados.

## 2. Ports de salida (interfaces en pkg/agent)

- [x] 2.1 Crear `pkg/agent/llm_port.go` con `package agent`. Definir `type ToolDefinition struct { Name string; Description string; Parameters map[string]any }` (Parameters es un JSON Schema de tipo objeto que describe los argumentos esperados de la tool). Criterio de listo: struct exportado, compila con `any` (requiere Go >= 1.18, confirmar `go.mod` declara `go 1.21` o superior).
- [x] 2.2 En el mismo archivo, definir `type LLMRequest struct { Messages []Message; Tools []ToolDefinition }` y `type LLMResponse struct { Message Message; FinishReason string }`.
- [x] 2.3 En el mismo archivo, definir la interfaz `type LLMClient interface { CompleteChat(ctx context.Context, req LLMRequest) (LLMResponse, error) }`. Importar `context` de librería estándar. Criterio de listo: la interfaz tiene exactamente un método.
- [x] 2.4 Crear `pkg/agent/mongo_port.go` con `package agent`. Definir `type CollectionInfo struct { Name string }` y `type FieldSample struct { Field string; Types []string; ExampleValue string }`.
- [x] 2.5 En el mismo archivo, definir la interfaz `type ReadOnlyMongoRepository interface` con exactamente estos cuatro métodos y ningún método adicional:
  ```go
  ListCollections(ctx context.Context) ([]CollectionInfo, error)
  DescribeCollection(ctx context.Context, collection string, sampleSize int) ([]FieldSample, error)
  Find(ctx context.Context, collection string, filterJSON string, projectionJSON string, limit int) (string, error)
  Aggregate(ctx context.Context, collection string, pipelineJSON string, limit int) (string, error)
  ```
  Criterio de listo (invariante de seguridad, NO negociable): la interfaz no contiene ningún método cuyo nombre empiece por `Insert`, `Update`, `Delete`, `Replace`, `Drop`, `Write`, ni ningún método que reciba un documento a persistir. `grep -iE '(Insert|Update|Delete|Replace|Drop)' pkg/agent/mongo_port.go` debe devolver vacío.
- [x] 2.6 Crear `pkg/agent/session_port.go` con `package agent`. Definir la interfaz `type SessionStore interface { AppendMessage(ctx context.Context, sessionID string, msg Message) error; GetHistory(ctx context.Context, sessionID string) ([]Message, error); ClearSession(ctx context.Context, sessionID string) error }`. Criterio de listo: interfaz con exactamente esos tres métodos.
- [x] 2.7 Verificar que ninguno de los tres archivos de puertos (`llm_port.go`, `mongo_port.go`, `session_port.go`) importa `gin`, `mongo-driver`, `redis` ni ningún cliente HTTP concreto — solo `context` y tipos de `agent.go`. Criterio de listo: `grep -E '"(go.mongodb.org|redis|gin-gonic)"' pkg/agent/*_port.go` no devuelve resultados.

## 3. Application / Caso de uso (pkg/agent/service.go y pkg/agent/tools.go)

- [x] 3.1 Crear `pkg/agent/tools.go` con `package agent`. Definir constantes de nombre de tool: `ToolListCollections = "list_collections"`, `ToolDescribeCollection = "describe_collection"`, `ToolQueryFind = "query_find"`, `ToolQueryAggregate = "query_aggregate"`.
- [x] 3.2 En el mismo archivo, definir la función `func BuildToolDefinitions(sampleSize int, maxResultLimit int) []ToolDefinition` que devuelve las 4 `ToolDefinition` (list_collections sin parámetros; describe_collection con parámetro requerido `collection: string`; query_find con parámetros `collection: string` (requerido), `filter: object` (requerido), `projection: object` (opcional), `limit: integer` (opcional); query_aggregate con parámetros `collection: string` (requerido), `pipeline: array` (requerido), `limit: integer` (opcional)). Cada `Description` debe explicar en texto que la tool es de solo lectura. Criterio de listo: la función devuelve un slice de longitud 4 y cada `ToolDefinition.Name` coincide con una constante de 3.1.
- [x] 3.3 En el mismo archivo, definir la función `func DispatchToolCall(ctx context.Context, repo ReadOnlyMongoRepository, sampleSize int, maxResultLimit int, call ToolCall) (string, bool)` que: parsea `call.Arguments` (JSON) según el nombre de la tool (`call.Name`), invoca el método correspondiente de `repo`, y devuelve `(resultadoJSON string, isError bool)`. Si `call.Name` no coincide con ninguna constante conocida, debe devolver `isError=true` con un mensaje `"unknown tool: <name>"`. Si el parseo de `Arguments` falla, debe devolver `isError=true` con un mensaje describiendo el error de parseo, sin hacer panic. Criterio de listo: la función nunca hace panic ante JSON malformado (se valida en el test de la sección 7.2).
- [x] 3.4 En `pkg/agent/tools.go`, dentro de la rama de `query_aggregate` de `DispatchToolCall`, antes de invocar `repo.Aggregate`, inspeccionar el pipeline parseado y rechazar (devolver `isError=true`) si algún stage del pipeline tiene como clave `$out` o `$merge`. Usar el helper de error `utils.ErrDisallowedPipelineStage(stageName string) error` (a crear en la tarea 8.7) para construir el mensaje. Criterio de listo: existe una rama explícita de validación antes de la llamada a `repo.Aggregate`, cubierta por el test de la sección 7.3.
- [x] 3.5 En `pkg/agent/tools.go`, cuando `limit` no se especifica o excede `maxResultLimit` en `query_find`/`query_aggregate`, `DispatchToolCall` debe forzar `limit = maxResultLimit` antes de invocar al `repo`. Criterio de listo: cubierto por el test de la sección 7.4.
- [x] 3.6 Crear `pkg/agent/service.go` con `package agent`. Definir la interfaz (puerto de entrada/driving) `type AgentService interface { Ask(ctx context.Context, q Question) (Answer, error) }`.
- [x] 3.7 En el mismo archivo, definir el struct privado de implementación:
  ```go
  type service struct {
      llm            LLMClient
      mongo          ReadOnlyMongoRepository
      sessions       SessionStore
      maxIterations  int
      requestTimeout time.Duration
      sampleSize     int
      maxResultLimit int
      systemPrompt   string
  }
  ```
  y el constructor `func NewAgentService(llm LLMClient, mongo ReadOnlyMongoRepository, sessions SessionStore, maxIterations int, requestTimeout time.Duration, sampleSize int, maxResultLimit int, systemPrompt string) AgentService`. Criterio de listo: constructor devuelve `&service{...}` con todos los campos asignados por posición.
- [x] 3.8 Implementar `func (s *service) Ask(ctx context.Context, q Question) (Answer, error)`:
  1. Validar que `q.Text` no esté vacío; si lo está, devolver `utils.ErrEmptyQuestion()`.
  2. Crear un `ctx` derivado con `context.WithTimeout(ctx, s.requestTimeout)` y `defer cancel()`.
  3. Recuperar historial con `s.sessions.GetHistory(ctx, q.SessionID)`; si falla, devolver el error envuelto (no continuar).
  4. Construir el mensaje de usuario `Message{Role: RoleUser, Content: q.Text, CreatedAt: time.Now()}`, añadirlo al historial en memoria y persistirlo con `s.sessions.AppendMessage`.
  5. Ejecutar un loop de hasta `s.maxIterations` iteraciones: en cada iteración, llamar `s.llm.CompleteChat(ctx, LLMRequest{Messages: historialActual, Tools: BuildToolDefinitions(s.sampleSize, s.maxResultLimit)})`; si `err != nil`, devolver el error envuelto; si la respuesta no tiene `ToolCalls`, persistir el mensaje del asistente y devolver `Answer{SessionID: q.SessionID, Text: resp.Message.Content, ToolCallsExecuted: <contador>}, nil`; si tiene `ToolCalls`, para cada una invocar `DispatchToolCall`, construir un `Message{Role: RoleTool, ToolCallID: call.ID, Content: resultadoJSON}`, persistirlo y añadirlo al historial en memoria, e incrementar el contador de tool calls ejecutadas.
  6. Si el loop termina sin respuesta final tras `s.maxIterations` iteraciones, devolver `utils.ErrToolLoopExceeded()`.
  7. Si `ctx.Err() == context.DeadlineExceeded` en cualquier punto, devolver `utils.ErrRequestTimeout()`.
  Criterio de listo: cubierto por los 6 casos de test descritos en la sección 7.1.
- [x] 3.9 Verificar que `pkg/agent/service.go` y `pkg/agent/tools.go` solo importan de terceros paquetes ya definidos dentro de `pkg/agent` y `pkg/utils`, más librería estándar (`context`, `encoding/json`, `time`, `fmt`). Criterio de listo: `grep -E '"(go.mongodb.org|redis|gin-gonic|net/http)"' pkg/agent/service.go pkg/agent/tools.go` no devuelve resultados.

## 4. Adapters de salida (driven)

- [x] 4.1 Crear `pkg/persistence/mongodb/readonly_repository.go` con `package mongodb`. Importar `go.mongodb.org/mongo-driver/mongo`, `go.mongodb.org/mongo-driver/bson`, y `agent "github.com/HongXiangZuniga/mongo-agent/pkg/agent"`. Definir `type storage struct { db *mongo.Database; queryTimeout time.Duration }` y el constructor `func NewReadOnlyRepository(db *mongo.Database, queryTimeout time.Duration) agent.ReadOnlyMongoRepository { return &storage{db, queryTimeout} }`.
- [x] 4.2 Implementar `func (s *storage) ListCollections(ctx context.Context) ([]agent.CollectionInfo, error)` usando `s.db.ListCollectionNames(ctx, bson.M{})`, aplicando `context.WithTimeout(ctx, s.queryTimeout)`, y mapeando cada nombre a `agent.CollectionInfo{Name: name}`. Criterio de listo: no ejecuta ninguna otra operación sobre `s.db` además de listar nombres.
- [x] 4.3 Implementar `func (s *storage) DescribeCollection(ctx context.Context, collection string, sampleSize int) ([]agent.FieldSample, error)` usando un pipeline de agregación de solo lectura `[{"$sample": {"size": sampleSize}}]` sobre `s.db.Collection(collection)`, decodificando cada documento a `bson.M`, y acumulando por cada campo observado: los tipos Go detectados (vía `fmt.Sprintf("%T", valor)`) y un valor de ejemplo (el primero encontrado, convertido a string con `fmt.Sprintf("%v", valor)`). Devolver el resultado como slice de `agent.FieldSample` ordenado alfabéticamente por `Field`. Si la colección no existe o no tiene documentos, devolver slice vacío sin error.
- [x] 4.4 Implementar `func (s *storage) Find(ctx context.Context, collection string, filterJSON string, projectionJSON string, limit int) (string, error)`: parsear `filterJSON` y `projectionJSON` (si no vacío) a `bson.M` con `bson.UnmarshalExtJSON`; si el parseo falla, devolver el error sin llamar al driver; ejecutar `s.db.Collection(collection).Find(ctx, filter, options.Find().SetProjection(projection).SetLimit(int64(limit)))`; decodificar todos los resultados a `[]bson.M`; serializar el slice resultante a JSON con `json.Marshal` y devolverlo como string.
- [x] 4.5 Implementar `func (s *storage) Aggregate(ctx context.Context, collection string, pipelineJSON string, limit int) (string, error)`: parsear `pipelineJSON` a `[]bson.M` (o `bson.A`); añadir un stage `{"$limit": limit}` al final del pipeline recibido si no lo tiene ya; ejecutar `s.db.Collection(collection).Aggregate(ctx, pipeline)`; decodificar y serializar igual que en 4.4. IMPORTANTE: esta función NO debe validar los stages `$out`/`$merge` (esa validación ya ocurre en `pkg/agent/tools.go` antes de llegar aquí — no duplicar la lógica de negocio en el adapter).
- [x] 4.6 Verificar que `readonly_repository.go` no define ningún método `Insert*`, `Update*`, `Delete*`, `Replace*` ni invoca `collection.InsertOne`, `collection.UpdateOne`, `collection.DeleteOne`, `collection.DeleteMany`, `collection.ReplaceOne`, `collection.Drop` en ningún punto del archivo. Criterio de listo: `grep -iE '\.(InsertOne|InsertMany|UpdateOne|UpdateMany|DeleteOne|DeleteMany|ReplaceOne|Drop)\(' pkg/persistence/mongodb/readonly_repository.go` no devuelve resultados.
- [x] 4.7 Crear `pkg/persistence/mongodb/readonly_guard.go` con `package mongodb`. Implementar `func VerifyReadOnlyGuarantee(ctx context.Context, db *mongo.Database) error`: intenta `db.Collection("__readonly_check__").InsertOne(ctx, bson.M{"canary": true, "ts": time.Now()})`; si la inserción **tiene éxito**, borrar inmediatamente ese documento de prueba (best-effort, ignorando el error del borrado) y devolver un error `utils.ErrMongoUserNotReadOnly()` (a crear en 8.7); si la inserción **falla** con un error de autorización de MongoDB (código de error `Unauthorized` o mensaje que contenga `"not authorized"`), devolver `nil` (la verificación pasó: el usuario efectivamente no puede escribir); si falla por cualquier otro motivo (red, timeout), devolver ese error tal cual (no se pudo verificar, no se asume nada). Criterio de listo: cubierto por el test de integración de la sección 7.6.
- [x] 4.8 Crear `pkg/persistence/redis/session_store.go` con `package redis`. Importar `github.com/redis/go-redis/v9` y `agent "github.com/HongXiangZuniga/mongo-agent/pkg/agent"`. Definir `type store struct { client *redis.Client; ttl time.Duration }` y el constructor `func NewSessionStore(client *redis.Client, ttl time.Duration) agent.SessionStore { return &store{client, ttl} }`.
- [x] 4.9 Implementar `func (s *store) AppendMessage(ctx context.Context, sessionID string, msg agent.Message) error`: serializar `msg` a JSON, hacer `RPUSH` a la clave `fmt.Sprintf("session:%s:messages", sessionID)`, y en la misma llamada (o inmediatamente después) ejecutar `EXPIRE` sobre esa clave con `s.ttl`. Usar `s.client.Pipelined` para ejecutar `RPUSH` + `Expire` en una sola ida y vuelta a Redis.
- [x] 4.10 Implementar `func (s *store) GetHistory(ctx context.Context, sessionID string) ([]agent.Message, error)`: ejecutar `LRANGE key 0 -1`, deserializar cada elemento de JSON a `agent.Message`, devolver el slice ordenado en el mismo orden almacenado (orden cronológico). Si la clave no existe (sesión expirada o nueva), devolver un slice vacío sin error.
- [x] 4.11 Implementar `func (s *store) ClearSession(ctx context.Context, sessionID string) error` ejecutando `DEL` sobre la clave de la sesión.
- [x] 4.12 Crear `pkg/llm/opencodezen/client.go` con `package opencodezen`. Definir `type client struct { httpClient *http.Client; baseURL string; apiKey string; model string }` y el constructor `func NewClient(httpClient *http.Client, baseURL string, apiKey string, model string) agent.LLMClient { return &client{httpClient, baseURL, apiKey, model} }`. Importar `agent "github.com/HongXiangZuniga/mongo-agent/pkg/agent"`.
- [x] 4.13 Definir en el mismo archivo (o en `pkg/llm/opencodezen/dto.go`) los tipos privados de request/response del formato OpenAI-compatible: `chatCompletionRequest{Model string; Messages []chatMessage; Tools []chatTool}`, `chatMessage{Role string; Content string; ToolCalls []chatToolCall; ToolCallID string}`, `chatTool{Type string; Function chatFunction}`, `chatFunction{Name string; Description string; Parameters map[string]any}`, `chatToolCall{ID string; Type string; Function chatFunctionCall}`, `chatFunctionCall{Name string; Arguments string}`, y el tipo de respuesta `chatCompletionResponse{Choices []chatChoice}` con `chatChoice{Message chatMessage; FinishReason string}`. Criterio de listo: estos tipos son privados (no exportados) y viven únicamente en `pkg/llm/opencodezen`; ningún tipo de `pkg/agent` los referencia.
- [x] 4.14 Implementar `func (c *client) CompleteChat(ctx context.Context, req agent.LLMRequest) (agent.LLMResponse, error)`: mapear `req.Messages`/`req.Tools` (tipos de `pkg/agent`) a `chatCompletionRequest` (tipos privados de 4.13); serializar a JSON; construir `http.NewRequestWithContext(ctx, "POST", c.baseURL+"/chat/completions", body)`; setear headers `Authorization: "Bearer " + c.apiKey` y `Content-Type: application/json`; ejecutar con `c.httpClient.Do`; si el código de estado HTTP es >= 400, leer el cuerpo del error y devolver un error descriptivo (sin incluir `c.apiKey` en el mensaje de error); deserializar la respuesta exitosa y mapear el primer `chatChoice` a `agent.LLMResponse` (incluyendo el mapeo de `ToolCalls` de vuelta a `agent.ToolCall`). Criterio de listo: en ningún punto del código el valor de `apiKey` se escribe en un log o en un mensaje de error.
- [x] 4.15 Verificar que `pkg/llm/opencodezen/client.go` no depende de ningún SDK de OpenAI de terceros — solo `net/http`, `encoding/json` y librería estándar. Criterio de listo: `grep -iE '"(github.com/(sashabaranov|openai))' pkg/llm/opencodezen/*.go` no devuelve resultados.

## 5. Adapter de entrada (HTTP)

- [x] 5.1 Crear `pkg/http/rest/rest.go` con `package rest`. Definir `type Response struct { Code int \`json:"code"\`; Message string \`json:"message"\`; Data interface{} \`json:"data"\` }` (idéntico patrón al de `CrudGoExample`).
- [x] 5.2 Crear `pkg/http/rest/router.go` con `package rest`. Implementar `func NewHandler(agentHandler AgentHandlers, apiToken string) *gin.Engine` que crea un `gin.Default()`, registra `POST /ask` protegida por el middleware de autenticación, y lo devuelve.
- [x] 5.3 En el mismo archivo, implementar `func TokenAuthMiddleware(requiredToken string) gin.HandlerFunc` que compara el header `Authorization` de la petición contra `requiredToken`; si están vacíos o no coinciden, responde `401` con un `Response{Code: 401, Message: "invalid or missing API token"}` y llama a `c.AbortWithStatusJSON`; si coincide, llama a `c.Next()`.
- [x] 5.4 Crear `pkg/http/rest/agent.go` con `package rest`. Definir la interfaz `type AgentHandlers interface { AskQuestion(*gin.Context) }` y el struct `type agentPort struct { agentService agent.AgentService }` con constructor `func NewAgentHandler(agentService agent.AgentService) AgentHandlers`.
- [x] 5.5 Definir en el mismo archivo el DTO de entrada `type askRequest struct { SessionID string \`json:"session_id"\`; Question string \`json:"question"\` }` y el DTO de salida `type askResponse struct { SessionID string \`json:"session_id"\`; Answer string \`json:"answer"\` }`.
- [x] 5.6 Implementar `func (p *agentPort) AskQuestion(ctx *gin.Context)`: parsear el body JSON a `askRequest` con `ctx.ShouldBindJSON`; si falla o `Question` está vacío, responder `400` con `Response{Code: 400, Message: "question is required"}`; si `SessionID` está vacío, generar uno nuevo con `github.com/google/uuid` (`uuid.NewString()`); invocar `p.agentService.Ask(ctx.Request.Context(), agent.Question{SessionID: sessionID, Text: req.Question})`; mapear los errores de dominio (`utils.ErrToolLoopExceeded`, `utils.ErrRequestTimeout`, errores de `SessionStore`/`LLMClient`) a los códigos HTTP `502`/`504`/`503` respectivamente descritos en el spec; en caso de éxito, responder `200` con `Response{Code: 200, Data: askResponse{SessionID: answer.SessionID, Answer: answer.Text}}`.
- [x] 5.7 Verificar que ningún mensaje de error devuelto por `AskQuestion` incluye el contenido de `err.Error()` sin filtrar cuando el error proviene de un adapter externo (Mongo/Redis/LLM) — usar siempre un mensaje genérico fijo para esos casos, y loguear el `err.Error()` real solo por `log.Printf` en servidor. Criterio de listo: cubierto por el test de la sección 7.7.

## 6. Infraestructura / Wiring (composition root)

- [x] 6.1 Crear `cmd/server/api.go` con `package main`. En `func init()`, cargar `.env` con `godotenv.Load()` (ignorando el error si el archivo no existe, igual que `CrudGoExample`) y leer todas las variables de entorno descritas en `.env.example` (tarea 9.4) a variables package-level, validando que `API_TOKEN`, `MONGODB_URI`, `MONGODB_DB_NAME`, `OPENCODE_API_KEY` no estén vacías; si alguna falta, `log.Fatal` con un mensaje indicando cuál falta (sin imprimir valores de otras variables sensibles).
- [x] 6.2 En el mismo archivo, implementar `func initMongo(uri, dbName string) *mongo.Database` siguiendo el mismo patrón que `CrudGoExample` (`mongo.Connect` + `client.Ping` con `readpref.Primary()`, `os.Exit(0)` si falla), usando `context.WithTimeout` de 10 segundos.
- [x] 6.3 En el mismo archivo, implementar `func initRedis(addr, password string, db int) *redis.Client` usando `redis.NewClient(&redis.Options{...})` seguido de un `client.Ping(ctx)` para validar la conexión al arrancar; `log.Fatal` si falla.
- [x] 6.4 En `func main()`: en orden: (a) inicializar Mongo con `initMongo`; (b) ejecutar `mongodb.VerifyReadOnlyGuarantee(ctx, mdb)` y hacer `log.Fatal` si devuelve error (invariante de solo lectura, no negociable — el proceso NO debe seguir arrancando); (c) inicializar Redis con `initRedis`; (d) construir el adapter Mongo con `mongodb.NewReadOnlyRepository(mdb, queryTimeout)`; (e) construir el adapter Redis con `redis.NewSessionStore(redisClient, sessionTTL)`; (f) construir el adapter LLM con `opencodezen.NewClient(&http.Client{Timeout: ...}, baseURL, apiKey, model)`; (g) construir `agent.NewAgentService(llmClient, mongoRepo, sessionStore, maxIterations, requestTimeout, sampleSize, maxResultLimit, systemPrompt)`; (h) construir `rest.NewAgentHandler(agentService)` y `rest.NewHandler(agentHandler, apiToken)`; (i) arrancar el servidor con `r.Run(":" + port)`.
- [x] 6.5 Definir en el mismo archivo (o en una constante en `pkg/agent`) el `systemPrompt` inicial pasado al agente: debe indicar explícitamente en texto que el agente SOLO puede leer datos, que debe usar `list_collections`/`describe_collection` antes de asumir un esquema, y que debe responder en español de forma concisa. Criterio de listo: el string contiene las palabras "solo lectura" o "read-only".
- [x] 6.6 Verificar que `cmd/server/api.go` es el ÚNICO archivo del proyecto que importa simultáneamente `pkg/agent`, `pkg/persistence/mongodb`, `pkg/persistence/redis`, `pkg/llm/opencodezen` y `pkg/http/rest`. Criterio de listo: `grep -rl "pkg/persistence/mongodb\"" --include=*.go . | grep -v _test.go` devuelve únicamente `cmd/server/api.go` y `pkg/persistence/mongodb/*` (self-import no aplica).

## 7. Tests

- [x] 7.1 Crear `test/agent_service_test.go`, `package test`. Definir fakes manuales (sin librerías de mocking): `fakeLLM` (implementa `agent.LLMClient`, configurable con una secuencia de `agent.LLMResponse`/`error` a devolver en llamadas sucesivas), `fakeMongoRepo` (implementa `agent.ReadOnlyMongoRepository` devolviendo datos fijos), `fakeSessionStore` (implementa `agent.SessionStore` en memoria con un `map[string][]agent.Message`). Escribir los tests: `TestAsk_DirectAnswerWithoutTools`, `TestAsk_SingleToolCallThenAnswer`, `TestAsk_MultipleIterations`, `TestAsk_ExceedsMaxIterations` (espera `utils.ErrToolLoopExceeded`), `TestAsk_LLMError` (el `fakeLLM` devuelve error, se espera que `Ask` propague un error), `TestAsk_EmptyQuestion` (espera `utils.ErrEmptyQuestion`). Criterio de listo: `go test ./test/... -run TestAsk` pasa en verde.
- [x] 7.2 En `test/agent_tools_test.go`, `package test`, escribir `TestDispatchToolCall_UnknownTool` (espera `isError=true`) y `TestDispatchToolCall_MalformedArguments` (JSON inválido en `Arguments`, espera `isError=true` y ausencia de panic, usando `defer recover()` en el test para detectar panics no capturados). Criterio de listo: `go test ./test/... -run TestDispatchToolCall` pasa en verde.
- [x] 7.3 En el mismo archivo, escribir `TestDispatchToolCall_RejectsOutStage` y `TestDispatchToolCall_RejectsMergeStage`: invocan `DispatchToolCall` con `ToolQueryAggregate` y un pipeline JSON que contiene `{"$out": "otra_coleccion"}` / `{"$merge": {...}}` respectivamente, y verifican que el `fakeMongoRepo.Aggregate` NUNCA es invocado (usar un contador de llamadas en el fake) y que el resultado tiene `isError=true`.
- [x] 7.4 En el mismo archivo, escribir `TestDispatchToolCall_ForcesMaxResultLimit`: invoca `DispatchToolCall` con `query_find` sin `limit` en los argumentos y `maxResultLimit=50`, y verifica (vía un `fakeMongoRepo` que registra el `limit` recibido) que `Find` fue invocado con `limit=50`.
- [x] 7.5 Crear `test/http_rest_test.go`, `package test`. Usando `net/http/httptest` y un `fakeAgentService` (implementa `agent.AgentService`), escribir: `TestAskQuestion_MissingAuthHeader` (espera `401`), `TestAskQuestion_InvalidToken` (espera `401`), `TestAskQuestion_MissingQuestionField` (espera `400`), `TestAskQuestion_Success` (espera `200` y cuerpo JSON con `data.answer`), `TestAskQuestion_GeneratesSessionIDWhenMissing` (espera `200` y `data.session_id` no vacío). Criterio de listo: `go test ./test/... -run TestAskQuestion` pasa en verde.
- [x] 7.6 Crear `test/integration/mongodb_readonly_test.go`, `package integration`, con build tag `//go:build integration` en la primera línea. Escribir `TestVerifyReadOnlyGuarantee_RealCluster`: si `MONGODB_URI` no está definida en el entorno, hacer `t.Skip("MONGODB_URI no configurada")`; si está definida, conectar contra el clúster real configurado, invocar `mongodb.VerifyReadOnlyGuarantee` y verificar que devuelve `nil` (asumiendo que el usuario configurado es efectivamente de rol `read`). Criterio de listo: `go test -tags=integration ./test/integration/... -run TestVerifyReadOnlyGuarantee` pasa en verde cuando se ejecuta con credenciales reales de solo lectura, y se salta (no falla) sin credenciales.
- [x] 7.7 Crear `test/integration/redis_session_test.go`, `package integration`, mismo build tag. Escribir `TestSessionStore_AppendAndGetHistory_RealRedis`: si `REDIS_ADDR` no está definida, `t.Skip`; si está definida, escribir 2 mensajes con `AppendMessage`, leer con `GetHistory` y verificar que devuelve los 2 mensajes en orden, luego `ClearSession` y verificar que `GetHistory` devuelve slice vacío.
- [x] 7.8 Crear `test/integration/llm_opencodezen_test.go`, `package integration`, mismo build tag. Escribir `TestCompleteChat_RealOpenCodeZen`: si `OPENCODE_API_KEY` no está definida, `t.Skip`; si está definida, enviar un `LLMRequest` simple (un mensaje de sistema + un mensaje de usuario, sin tools) y verificar que `CompleteChat` devuelve un `LLMResponse.Message.Content` no vacío y `err == nil`.
- [x] 7.9 Añadir al `Makefile` (tarea 9.3) los targets `unit-test` (`go test ./test/...` excluyendo el paquete `integration` por defecto, ya que ese paquete requiere el build tag) e `integration-test` (`go test -tags=integration ./test/integration/...`).

## 8. Utilidades y errores de dominio

- [x] 8.1 Crear `pkg/utils/errors.go` con `package utils`. Definir `func ErrEmptyQuestion() error { return errors.New("question is required") }`.
- [x] 8.2 En el mismo archivo, `func ErrToolLoopExceeded() error { return errors.New("agent did not produce a final answer within the maximum number of tool-call iterations") }`.
- [x] 8.3 En el mismo archivo, `func ErrRequestTimeout() error { return errors.New("request exceeded the maximum allowed processing time") }`.
- [x] 8.4 En el mismo archivo, `func ErrCollectionNotFound(name string) error { return fmt.Errorf("collection %q not found", name) }`.
- [x] 8.5 En el mismo archivo, `func ErrDisallowedPipelineStage(stage string) error { return fmt.Errorf("aggregation stage %q is not allowed: read-only agent", stage) }`.
- [x] 8.6 En el mismo archivo, `func ErrSessionStoreUnavailable(cause error) error { return fmt.Errorf("session store unavailable: %w", cause) }` y `func ErrLLMProviderUnavailable(cause error) error { return fmt.Errorf("llm provider unavailable: %w", cause) }`.
- [x] 8.7 En el mismo archivo, `func ErrMongoUserNotReadOnly() error { return errors.New("configured mongodb user is NOT read-only: a write operation unexpectedly succeeded") }`.
- [x] 8.8 Verificar que ninguna función de `pkg/utils/errors.go` interpola directamente un valor que pueda contener una credencial (URIs, tokens, API keys) en el mensaje de error. Criterio de listo: revisión manual del archivo; ningún parámetro llamado `uri`, `token`, `apiKey`, `password` se usa en `fmt.Errorf`.

## 9. Scaffold del repositorio y documentación

- [x] 9.1 Verificar que `go.mod` declara `module github.com/HongXiangZuniga/mongo-agent` y `go 1.22` (o superior), y ejecutar `go mod tidy` para resolver y fijar las versiones reales de `github.com/gin-gonic/gin`, `go.mongodb.org/mongo-driver`, `github.com/redis/go-redis/v9`, `github.com/joho/godotenv`, `github.com/google/uuid`. Criterio de listo: `go build ./...` termina sin errores y se genera `go.sum`.
- [x] 9.2 Completar `.env.example` (ya scaffoldeado) verificando que incluye TODAS las variables usadas por `cmd/server/api.go`, cada una con un valor placeholder (nunca una credencial real): `PORT`, `API_TOKEN`, `MONGODB_URI`, `MONGODB_DB_NAME`, `MONGODB_QUERY_TIMEOUT_SECONDS`, `MONGODB_MAX_RESULT_LIMIT`, `MONGO_SAMPLE_SIZE`, `OPENCODE_API_KEY`, `OPENCODE_BASE_URL`, `OPENCODE_MODEL`, `AGENT_MAX_TOOL_ITERATIONS`, `AGENT_REQUEST_TIMEOUT_SECONDS`, `REDIS_ADDR`, `REDIS_PASSWORD`, `REDIS_DB`, `SESSION_TTL_SECONDS`.
- [x] 9.3 Completar el `Makefile` (ya scaffoldeado) con los targets `run`, `install`, `build`, `unit-test`, `integration-test`, `docker-build`, `docker-run`, siguiendo el mismo estilo que `CrudGoExample`.
- [x] 9.4 Completar `build/docker/Dockerfile` (ya scaffoldeado) como build multi-stage: stage `builder` con una imagen `golang` cuya versión coincide con la declarada en `go.mod`, ejecuta `go build -o /app/api ./cmd/server`; stage final con una imagen base mínima (`gcr.io/distroless/base-debian12` o `alpine`) que copia el binario `/app/api` y lo ejecuta como `ENTRYPOINT`. Exponer el puerto definido en `PORT`.
- [x] 9.5 Completar `README.md` (ya scaffoldeado) describiendo: propósito del proyecto, arquitectura hexagonal (referenciar el diagrama de `openspec/changes/add-nl-mongo-agent/design.md`), variables de entorno requeridas (tabla, sin valores reales), cómo correr localmente (`docker-compose up -d` para Redis + `make run`), ejemplo de request `curl` a `POST /ask`, y una sección explícita "Garantía de solo lectura" explicando las 4 capas de protección descritas en `design.md`.
- [x] 9.6 Verificar que `.gitignore` (ya scaffoldeado) excluye `.env`, binarios (`/api`, `*.exe`), y no excluye `.env.example`.

## 10. Validación final

- [x] 10.1 Ejecutar `gofmt -l .` desde la raíz del proyecto y corregir cualquier archivo listado (debe devolver una lista vacía).
- [x] 10.2 Ejecutar `go vet ./...` y corregir cualquier advertencia.
- [x] 10.3 Ejecutar `go build ./...` y confirmar que compila sin errores.
- [x] 10.4 Ejecutar `make unit-test` (equivalente a `go test ./test/...` excluyendo el paquete `integration`) y confirmar que todos los tests pasan.
- [x] 10.5 Ejecutar `npx @fission-ai/openspec@latest validate add-nl-mongo-agent --strict` desde la raíz del repo y corregir cualquier error de formato reportado antes de considerar el cambio listo para `openspec archive add-nl-mongo-agent`.
