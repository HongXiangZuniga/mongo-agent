## 1. Dominio de autenticación (pkg/auth)

- [x] 1.1 Crear `pkg/auth/auth.go` con `package auth`. Definir el struct `type User struct { Username string; PasswordHash string }` y la variable de paquete `var ErrInvalidCredentials = errors.New("invalid credentials")` (import `"errors"`). Añadir el comentario de paquete: `// Package auth contiene el dominio, los puertos y el caso de uso del login de usuario/contraseña. No importa infraestructura (ni mongo, ni bcrypt, ni os).`. Criterio de listo: `gofmt -l pkg/auth/auth.go` no reporta errores; `grep -n 'ErrInvalidCredentials' pkg/auth/auth.go` devuelve 1 línea.
- [x] 1.2 Verificar que `pkg/auth/auth.go` no importa infraestructura. Criterio de listo: `grep -nE '"go.mongodb.org|bcrypt|"os"' pkg/auth/auth.go` no devuelve ninguna línea.

## 2. Puertos (pkg/auth)

- [x] 2.1 Crear `pkg/auth/authenticator.go` con `package auth`, import `"context"`, y la interfaz `type Authenticator interface { Authenticate(ctx context.Context, username, password string) (User, error) }`. Este es el puerto de entrada (driving) del caso de uso. Criterio de listo: compila; `gofmt -l pkg/auth/authenticator.go` no reporta errores.
- [x] 2.2 Crear `pkg/auth/user_port.go` con `package auth`, imports `"context"` y `"errors"`, la interfaz `type UserRepository interface { FindByUsername(ctx context.Context, username string) (User, error) }` (puerto de salida driven) y `var ErrUserNotFound = errors.New("user not found")`. Añadir comentario indicando el invariante: solo lee usuarios, jamás escribe. Criterio de listo: compila; `grep -n 'ErrUserNotFound' pkg/auth/user_port.go` devuelve 1 línea.
- [x] 2.3 Crear `pkg/auth/password_port.go` con `package auth` y la interfaz `type PasswordHasher interface { Compare(hashedPassword, plaintext string) error }` (puerto de salida driven; `Compare` devuelve `nil` si coinciden, error en caso contrario). Criterio de listo: compila; `gofmt -l pkg/auth/password_port.go` no reporta errores.

## 3. Caso de uso (pkg/auth)

- [x] 3.1 Crear `pkg/auth/service.go` con `package auth`, import `"context"`, el struct `type service struct { users UserRepository; hasher PasswordHasher }` y el constructor `func NewService(users UserRepository, hasher PasswordHasher) Authenticator { return &service{users: users, hasher: hasher} }`. Criterio de listo: compila.
- [x] 3.2 En `pkg/auth/service.go`, implementar `func (s *service) Authenticate(ctx context.Context, username, password string) (User, error)` con la lógica exacta de `design.md` sección D3: (a) `u, err := s.users.FindByUsername(ctx, username)`; si `err != nil` devolver `User{}, ErrInvalidCredentials`; (b) `if err := s.hasher.Compare(u.PasswordHash, password); err != nil` devolver `User{}, ErrInvalidCredentials`; (c) devolver `u, nil`. Nota: se devuelve siempre `ErrInvalidCredentials` (nunca `ErrUserNotFound`) hacia afuera, para no revelar si falló usuario o contraseña. Criterio de listo: compila; pasa los tests de la tarea 3.3.
- [x] 3.3 Crear `test/auth_service_test.go` con `package test` y fakes en el mismo archivo: `type fakeUserRepo struct { user auth.User; err error }` que en `FindByUsername` devuelve `f.user, f.err`; `type fakeHasher struct { err error }` que en `Compare` devuelve `f.err`. Añadir `TestAuthenticate_ValidCredentialsReturnsUser` (repo devuelve un `User{Username:"admin", PasswordHash:"h"}` sin error, hasher devuelve `nil` → `Authenticate` devuelve ese usuario y `err == nil`), `TestAuthenticate_WrongPasswordReturnsInvalidCredentials` (repo ok, hasher devuelve `errors.New("mismatch")` → `Authenticate` devuelve `auth.ErrInvalidCredentials` verificado con `errors.Is`), `TestAuthenticate_UnknownUserReturnsInvalidCredentials` (repo devuelve `auth.ErrUserNotFound` → `Authenticate` devuelve `auth.ErrInvalidCredentials`, NO `auth.ErrUserNotFound`). Criterio de listo: `go test ./test/... -run TestAuthenticate` pasa.

## 4. Adapter de contraseñas bcrypt (pkg/security)

- [x] 4.1 Crear `pkg/security/bcrypt.go` con `package security`, imports `"golang.org/x/crypto/bcrypt"` y `"github.com/HongXiangZuniga/mongo-agent/pkg/auth"`. Definir `type bcryptHasher struct{}`, `func NewBcryptHasher() auth.PasswordHasher { return bcryptHasher{} }`, y `func (bcryptHasher) Compare(hashedPassword, plaintext string) error { return bcrypt.CompareHashAndPassword([]byte(hashedPassword), []byte(plaintext)) }`. Criterio de listo: compila; `gofmt -l pkg/security/bcrypt.go` no reporta errores.
- [x] 4.2 Ejecutar `go mod tidy` para promover `golang.org/x/crypto` de indirecta a directa. Criterio de listo: `go build ./...` compila; `grep -n 'golang.org/x/crypto' go.mod` muestra la dependencia sin el comentario `// indirect`.
- [x] 4.3 Crear `test/security_bcrypt_test.go` con `package test` y `TestBcryptHasher_CompareMatchesAndRejects`: generar un hash con `bcrypt.GenerateFromPassword([]byte("admin123"), bcrypt.DefaultCost)`, construir `security.NewBcryptHasher()`, verificar que `Compare(string(hash), "admin123")` devuelve `nil` y que `Compare(string(hash), "wrong")` devuelve un error no nil. Criterio de listo: `go test ./test/... -run TestBcryptHasher` pasa.

## 5. Adapter de repositorio de usuarios (pkg/persistence/authmongo)

- [x] 5.1 Crear `pkg/persistence/authmongo/user_repository.go` con `package authmongo`, imports `"context"`, `"errors"`, `"time"`, `"go.mongodb.org/mongo-driver/bson"`, `"go.mongodb.org/mongo-driver/mongo"` y `"github.com/HongXiangZuniga/mongo-agent/pkg/auth"`. Añadir comentario de paquete indicando que es el adapter de salida hacia la base de usuarios, SEPARADO de `pkg/persistence/mongodb` (que tiene el invariante de solo lectura del agente), y que este repositorio solo lee usuarios. Criterio de listo: compila.
- [x] 5.2 En `pkg/persistence/authmongo/user_repository.go`, definir el DTO `type userDoc struct { Username string \`bson:"username"\`; PasswordHash string \`bson:"password_hash"\` }`, el struct `type repository struct { coll *mongo.Collection; queryTimeout time.Duration }` y el constructor `func NewUserRepository(db *mongo.Database, collectionName string, queryTimeout time.Duration) auth.UserRepository { return &repository{coll: db.Collection(collectionName), queryTimeout: queryTimeout} }`. Criterio de listo: compila.
- [x] 5.3 En `pkg/persistence/authmongo/user_repository.go`, implementar `func (r *repository) FindByUsername(ctx context.Context, username string) (auth.User, error)` con la lógica exacta de `design.md` sección D4: aplicar `context.WithTimeout(ctx, r.queryTimeout)`; `r.coll.FindOne(ctx, bson.M{"username": username}).Decode(&doc)`; si `errors.Is(err, mongo.ErrNoDocuments)` devolver `auth.User{}, auth.ErrUserNotFound`; si `err != nil` devolver `auth.User{}, err`; si no, devolver `auth.User{Username: doc.Username, PasswordHash: doc.PasswordHash}, nil`. Verificar que el archivo NO invoca `InsertOne`, `UpdateOne`, `DeleteOne`, `ReplaceOne` ni `Drop`. Criterio de listo: compila; `grep -nE 'InsertOne|UpdateOne|DeleteOne|ReplaceOne|Drop' pkg/persistence/authmongo/user_repository.go` no devuelve ninguna línea.
- [x] 5.4 Crear `test/integration/authmongo_test.go` con el build tag `//go:build integration` (primera línea) y `package integration`. Añadir `TestFindByUsername_ReturnsSeededUser` y `TestFindByUsername_UnknownReturnsErrUserNotFound` que se conectan a `AUTH_MONGODB_URI` (leído con `os.Getenv`; si está vacío, `t.Skip`), insertan un usuario de prueba, y verifican el comportamiento de `FindByUsername`. Criterio de listo: `go test -tags=integration ./test/integration/... -run TestFindByUsername` pasa cuando hay un Mongo de usuarios disponible; `go test ./test/...` (sin el tag) no compila ni ejecuta este archivo.

## 6. Adapter web: login por usuario/contraseña (pkg/http/web)

- [x] 6.1 En `pkg/http/web/handlers.go`, añadir el import `"github.com/HongXiangZuniga/mongo-agent/pkg/auth"`. Modificar el struct `webPort` para añadir el campo `authenticator auth.Authenticator`. Modificar `NewWebHandler` para que su firma sea `func NewWebHandler(agentService agent.AgentService, cookieCfg CookieConfig, scrubber *utils.SecretScrubber, authenticator auth.Authenticator) WebHandlers` y su cuerpo `return &webPort{agentService, cookieCfg, scrubber, authenticator}`. Criterio de listo: compila tras actualizar el call site (tarea 8.4).
- [x] 6.2 En `pkg/http/web/handlers.go`, reemplazar el cuerpo del método `Login` por la lógica de `design.md` sección D6: leer `username := c.PostForm("username")` y `password := c.PostForm("password")`; llamar `_, err := p.authenticator.Authenticate(c.Request.Context(), username, password)`; si `err != nil`, responder `c.Status(http.StatusUnauthorized)`, header `Content-Type: text/html; charset=utf-8`, y `render(c.Writer, "login", struct{ Error string }{Error: "Usuario o contraseña inválidos"})` y `return`; si `err == nil`, fijar la cookie EXACTAMENTE como el código actual (mismo `SetSameSite(http.SameSiteStrictMode)` y `SetCookie` con valor `p.cookieCfg.APIToken`, nombre `p.cookieCfg.CookieName`, `MaxAge`, path `/web`, `Secure`, `HttpOnly=true`) y `c.Redirect(http.StatusSeeOther, "/web")`. Quitar el import `"crypto/subtle"` de `handlers.go` si ya no se usa en ese archivo. Criterio de listo: compila; `grep -n 'PostForm("username")' pkg/http/web/handlers.go` y `grep -n 'PostForm("password")' pkg/http/web/handlers.go` devuelven cada uno 1 línea; `grep -n 'p.authenticator.Authenticate' pkg/http/web/handlers.go` devuelve 1 línea.
- [x] 6.3 Verificar que la cookie fija el valor `p.cookieCfg.APIToken` (no la contraseña ni el usuario). Criterio de listo: en el método `Login`, el segundo argumento de `c.SetCookie` es `p.cookieCfg.APIToken`; `grep -n 'password' pkg/http/web/handlers.go` no muestra ninguna línea donde `password` se pase a `SetCookie`.
- [x] 6.4 Confirmar que `pkg/http/web/middleware.go` (`CookieAuthMiddleware`), `pkg/http/web/config.go` (`CookieConfig`), el método `Logout` y `LoginForm` NO se modifican. Criterio de listo: revisión manual; `git diff --stat pkg/http/web/middleware.go pkg/http/web/config.go` no muestra cambios.

## 7. Plantilla de login con dos campos (pkg/http/web/templates)

- [x] 7.1 En `pkg/http/web/templates/login.html`, reemplazar el bloque `<form>` actual (un único input `token`) por un formulario con dos campos: un `<label for="username">Usuario</label>` + `<input type="text" id="username" name="username" required>` y un `<label for="password">Contraseña</label>` + `<input type="password" id="password" name="password" required>`, manteterniendo `method="POST" action="/web/login"` y el botón `<button type="submit" class="btn-primary">Ingresar</button>`. Conservar el resto de la plantilla (doctype, `<head>`, `login-box`, bloque `{{if .Error}}`) sin cambios. Criterio de listo: `grep -n 'name="username"' pkg/http/web/templates/login.html` y `grep -n 'name="password"' pkg/http/web/templates/login.html` devuelven cada uno 1 línea; `grep -n 'name="token"' pkg/http/web/templates/login.html` no devuelve ninguna línea.

## 8. Composition root: conexión de usuarios y wiring (cmd/server/api.go)

- [x] 8.1 En `cmd/server/api.go`, añadir a la lista de variables de paquete (bloque `var (...)`): `authMongodbURI string`, `authMongodbDBName string`, `authMongodbUsersColl string`. Criterio de listo: compila.
- [x] 8.2 En `cmd/server/api.go`, función `init()`, tras la lectura de las variables Mongo existentes, añadir: `authMongodbURI = getenv("AUTH_MONGODB_URI", "")`, `authMongodbDBName = getenv("AUTH_MONGODB_DB_NAME", "")`, `authMongodbUsersColl = getenv("AUTH_MONGODB_USERS_COLLECTION", "users")`. En el bloque de `requireEnv`, añadir `requireEnv("AUTH_MONGODB_URI", authMongodbURI)` y `requireEnv("AUTH_MONGODB_DB_NAME", authMongodbDBName)`. Criterio de listo: compila; `grep -n 'AUTH_MONGODB_URI' cmd/server/api.go` devuelve al menos 2 líneas (lectura + requireEnv).
- [x] 8.3 En `cmd/server/api.go`, añadir los imports `"github.com/HongXiangZuniga/mongo-agent/pkg/auth"`, `"github.com/HongXiangZuniga/mongo-agent/pkg/persistence/authmongo"` y `"github.com/HongXiangZuniga/mongo-agent/pkg/security"`. En `main()`, DESPUÉS del bloque de `VerifyReadOnlyGuarantee` sobre `mdb` (para no confundir la conexión del agente con la de usuarios) y de `initRedis`, añadir: `authDB := initMongo(authMongodbURI, authMongodbDBName)`, `userRepo := authmongo.NewUserRepository(authDB, authMongodbUsersColl, mongodbQueryTimeout)`, `passwordHasher := security.NewBcryptHasher()`, `authService := auth.NewService(userRepo, passwordHasher)`. Criterio de listo: compila; `grep -n 'authmongo.NewUserRepository' cmd/server/api.go` devuelve 1 línea; `grep -n 'auth.NewService' cmd/server/api.go` devuelve 1 línea.
- [x] 8.4 En `cmd/server/api.go`, actualizar la construcción `webHandler := web.NewWebHandler(agentService, web.CookieConfig{...}, secretScrubber)` para pasar `authService` como cuarto argumento: `web.NewWebHandler(agentService, web.CookieConfig{...}, secretScrubber, authService)`. Criterio de listo: compila; `go build ./...` no reporta errores.
- [x] 8.5 Verificar que `mongodb.VerifyReadOnlyGuarantee` se sigue invocando ÚNICAMENTE sobre la conexión del agente (`mdb`), nunca sobre `authDB`. Criterio de listo: `grep -n 'VerifyReadOnlyGuarantee' cmd/server/api.go` devuelve 1 línea y su argumento es `mdb`, no `authDB`.

## 9. Seed del usuario de prueba (build/mongo-auth)

- [x] 9.1 Generar un hash bcrypt de la contraseña `admin123` con un programa Go de un solo uso: crear un archivo temporal `scripts/genhash/main.go` con `package main` que importe `"fmt"` y `"golang.org/x/crypto/bcrypt"`, y cuyo `main()` sea `h, _ := bcrypt.GenerateFromPassword([]byte("admin123"), bcrypt.DefaultCost); fmt.Println(string(h))`. Ejecutar `go run ./scripts/genhash` desde la raíz del repo, copiar el hash impreso (empieza por `$2a$` o `$2b$`), y BORRAR el directorio `scripts/genhash` al terminar (no debe quedar en el repo). Criterio de listo: se dispone de una cadena de hash bcrypt válida de `admin123` (empieza por `$2a$` o `$2b$` y tiene 60 caracteres); el directorio `scripts/genhash` ya no existe.
- [x] 9.2 Crear `build/mongo-auth/seed.js` que, sobre la base activa (`authdb`, fijada por `MONGO_INITDB_DATABASE`), ejecute `db.users.updateOne({ username: "admin" }, { $set: { username: "admin", password_hash: "<HASH_GENERADO_EN_9.1>" } }, { upsert: true });` usando el hash generado en la tarea 9.1. El script NO debe contener la contraseña en texto plano en ninguna parte. Criterio de listo: `grep -n 'password_hash' build/mongo-auth/seed.js` devuelve 1 línea; `grep -n 'admin123' build/mongo-auth/seed.js` no devuelve ninguna línea; el valor de `password_hash` empieza por `$2`.

## 10. docker-compose: servicios api y mongo-auth (docker-compose.yml)

- [x] 10.1 En `docker-compose.yml`, añadir al servicio `redis` un `healthcheck` con `test: ["CMD", "redis-cli", "ping"]`, `interval: 5s`, `timeout: 3s`, `retries: 10`. Criterio de listo: `docker-compose config` (o `docker compose config`) no reporta error de sintaxis y muestra el healthcheck de redis.
- [x] 10.2 En `docker-compose.yml`, añadir el servicio `mongo-auth` exactamente como en `design.md` sección D8: `image: mongo:7`, `container_name: mongo-agent-auth-db`, `environment: [MONGO_INITDB_DATABASE=authdb]`, `volumes: [mongo_auth_data:/data/db, ./build/mongo-auth/seed.js:/docker-entrypoint-initdb.d/seed.js:ro]`, `ports: ["27017:27017"]`, `healthcheck` con `test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping').ok"]` (`interval: 5s`, `timeout: 5s`, `retries: 10`), y `networks: [agente_net]`. Criterio de listo: `docker-compose config` muestra el servicio `mongo-auth` con el volumen y el bind mount del seed.
- [x] 10.3 En `docker-compose.yml`, añadir el servicio `api` como en `design.md` sección D8: `build: { context: ., dockerfile: build/docker/Dockerfile }`, `container_name: mongo-agent-api`, `env_file: [.env]`, `environment: [REDIS_ADDR=redis:6379, AUTH_MONGODB_URI=mongodb://mongo-auth:27017, AUTH_MONGODB_DB_NAME=authdb, AUTH_MONGODB_USERS_COLLECTION=users]`, `ports: ["8080:8080"]`, `depends_on: { redis: { condition: service_healthy }, mongo-auth: { condition: service_healthy } }`, `networks: [agente_net]`. Criterio de listo: `docker-compose config` muestra el servicio `api` con su `build` y sus `depends_on` con `condition: service_healthy`.
- [x] 10.4 En `docker-compose.yml`, añadir la sección top-level `volumes:` con la entrada `mongo_auth_data:`. Criterio de listo: `docker-compose config` muestra el volumen `mongo_auth_data` declarado.
- [x] 10.5 En `.dockerignore`, confirmar que el contexto de build del servicio `api` no rompe por excluir archivos necesarios: el binario se construye desde el código Go (incluido), y `docker-compose.yml`/`Makefile`/`README.md` excluidos no afectan el build. Confirmar además que `build/mongo-auth/seed.js` NO se necesita dentro de la imagen `api` (se monta en el contenedor `mongo-auth` vía volumen, no se copia a la imagen Go). Criterio de listo: revisión manual; no se requiere cambio en `.dockerignore`.

## 11. Variables de entorno (.env.example, .env)

- [x] 11.1 En `.env.example`, añadir una nueva sección con comentario en español (mismo estilo que las existentes): `# --- MongoDB de autenticación (instancia LOCAL para el login web; separada del cluster Atlas de solo lectura del agente) ---` seguida de `AUTH_MONGODB_URI=mongodb://localhost:27017`, `AUTH_MONGODB_DB_NAME=authdb`, `AUTH_MONGODB_USERS_COLLECTION=users`. Nota en el comentario: dentro de docker-compose el host es `mongo-auth`, no `localhost` (compose lo sobreescribe). Criterio de listo: `grep -nE '^AUTH_MONGODB_URI=|^AUTH_MONGODB_DB_NAME=|^AUTH_MONGODB_USERS_COLLECTION=' .env.example` devuelve 3 líneas.
- [x] 11.2 Si existe `.env` local (no versionado), añadir las mismas tres variables para poder ejecutar `make run` localmente. Criterio de listo: `.env` contiene `AUTH_MONGODB_URI`, `AUTH_MONGODB_DB_NAME`, `AUTH_MONGODB_USERS_COLLECTION` (verificación local; `.env` no se commitea).

## 12. Documentación (README.md, Makefile)

- [x] 12.1 En `README.md`, sección "Correr localmente" (o una nueva subsección "Llegar y montar con docker-compose"), documentar que `docker-compose up` levanta Redis + base de usuarios (seedeada) + API Go, y que solo se requiere un `.env` con los secretos externos (`API_TOKEN`, `MONGODB_URI` de Atlas, `MONGODB_DB_NAME`, `OPENCODE_API_KEY`). Criterio de listo: `grep -ni 'docker-compose up' README.md` devuelve al menos 1 línea en el contexto del arranque integral.
- [x] 12.2 En `README.md`, añadir una subsección "Usuario de prueba (login web)" que documente las credenciales del usuario precargado: **Usuario:** `admin`, **Contraseña:** `admin123`, e indique que están pensadas solo para la POC y que la contraseña se almacena hasheada (bcrypt) en la base de usuarios. Criterio de listo: `grep -n 'admin123' README.md` devuelve al menos 1 línea; `grep -ni 'bcrypt' README.md` devuelve al menos 1 línea.
- [x] 12.3 En `README.md`, sección "Seguridad" o "Interfaz web de chat", actualizar la descripción del login web: pasa de "campo token único comparado contra API_TOKEN" a "usuario y contraseña validados contra una base MongoDB local; la cookie de sesión sigue siendo un marcador validado contra API_TOKEN; POST /ask no cambia (sigue por API_TOKEN)". Criterio de listo: `grep -ni 'usuario y contraseña\|usuario/contraseña' README.md` devuelve al menos 1 línea.
- [x] 12.4 En `README.md`, sección "Variables de entorno", documentar `AUTH_MONGODB_URI`, `AUTH_MONGODB_DB_NAME`, `AUTH_MONGODB_USERS_COLLECTION` y la nota de que la base de usuarios es una instancia local separada del Atlas de solo lectura. Criterio de listo: `grep -n 'AUTH_MONGODB_URI' README.md` devuelve al menos 1 línea.
- [x] 12.5 (Opcional, recomendado) En `Makefile`, añadir un target `compose-up` con `docker-compose up --build` para el arranque integral "llegar y montar". Criterio de listo: `grep -n 'compose-up' Makefile` devuelve 1 línea, o se documenta que el arranque estándar es `docker-compose up --build` en README.

## 13. Validación final

- [x] 13.1 Ejecutar `gofmt -l .` desde la raíz y confirmar que no reporta ningún archivo. Si reporta alguno, `gofmt -w <archivo>` y reverificar.
- [x] 13.2 Ejecutar `go build ./...` desde la raíz y confirmar que compila sin errores.
- [x] 13.3 Ejecutar `make unit-test` (o `go test ./test/...`) y confirmar que pasan todos los tests, incluyendo los nuevos de las tareas 3.3, 4.3 y los del handler web (`TestLogin_*` si se añadieron). Los tests de integración de la tarea 5.4 quedan fuera de `make unit-test` (requieren el tag `integration`).
- [ ] 13.4 (Manual, opcional pero recomendado) Ejecutar `docker-compose up --build` con un `.env` válido, esperar a que `mongo-auth` y `api` estén saludables, abrir `http://localhost:8080/web`, iniciar sesión con `admin` / `admin123` y confirmar que redirige a `/web`. Confirmar que credenciales incorrectas devuelven el formulario con error.
- [x] 13.5 Ejecutar `npx @fission-ai/openspec@latest validate add-user-login --strict` desde la raíz y confirmar que no reporta errores. Corregir cualquier error de formato en `proposal.md`, `design.md` o `specs/user-authentication/spec.md` antes de dar por completado el change.

## 14. Corrección: sesión web real respaldada en Redis

Contexto: las secciones 1-13 ya están implementadas, pero introdujeron un bug de
diseño (D2 original): la cookie web valía `API_TOKEN` (estático, único para todos)
y `CookieAuthMiddleware` la comparaba contra ese `API_TOKEN`, puenteando el login
de usuario/contraseña. Esta sección reemplaza ese modelo por sesiones reales de
usuario respaldadas en Redis (ver `design.md` D2, D6, D7, D10, D11, D12). NO se
toca `pkg/auth` `Authenticator`/`service.go` (validación de credenciales) ni el
adapter bcrypt ni el repositorio de usuarios: esos ya funcionan y no son el bug.

### 14a. Puerto y dominio de sesión web (pkg/auth)

- [x] 14.1 Crear `pkg/auth/web_session_port.go` con `package auth`, imports `"context"`, `"errors"`, `"time"`, según `design.md` D10: el struct `type WebSession struct { Username string; CreatedAt time.Time }`, la interfaz de puerto de salida `type WebSessionStore interface { Save(ctx context.Context, sessionID string, session WebSession) error; Find(ctx context.Context, sessionID string) (WebSession, error); Delete(ctx context.Context, sessionID string) error }` y `var ErrWebSessionNotFound = errors.New("web session not found")`. Añadir comentarios indicando que el TTL lo aplica el adapter. Criterio de listo: compila; `grep -n 'WebSessionStore' pkg/auth/web_session_port.go` devuelve 1 línea; `grep -n 'ErrWebSessionNotFound' pkg/auth/web_session_port.go` devuelve 1 línea.
- [x] 14.2 Verificar que `pkg/auth/web_session_port.go` no importa infraestructura. Criterio de listo: `grep -nE '"github.com/redis|go-redis|go.mongodb.org|"os"' pkg/auth/web_session_port.go` no devuelve ninguna línea.

### 14b. Caso de uso de sesión web (pkg/auth)

- [x] 14.3 Crear `pkg/auth/web_session.go` con `package auth`, imports `"context"`, `"crypto/rand"`, `"encoding/hex"`, `"time"`, según `design.md` D11: la interfaz de puerto de entrada `type WebSessionManager interface { Create(ctx context.Context, username string) (string, error); Validate(ctx context.Context, sessionID string) (WebSession, error); Revoke(ctx context.Context, sessionID string) error }`, el struct `type webSessionService struct { store WebSessionStore; now func() time.Time }`, el constructor `func NewWebSessionManager(store WebSessionStore) WebSessionManager { return &webSessionService{store: store, now: func() time.Time { return time.Now().UTC() }} }`, y los tres métodos con la lógica EXACTA de D11: `Create` genera 32 bytes con `rand.Read`, los codifica con `hex.EncodeToString` como `sessionID`, hace `store.Save(ctx, sessionID, WebSession{Username: username, CreatedAt: s.now()})` y devuelve `sessionID`; `Validate` devuelve `ErrWebSessionNotFound` si `sessionID == ""`, si no delega en `store.Find`; `Revoke` devuelve `nil` si `sessionID == ""`, si no delega en `store.Delete`. Criterio de listo: compila; `gofmt -l pkg/auth/web_session.go` no reporta errores; `grep -n 'crypto/rand' pkg/auth/web_session.go` devuelve 1 línea.
- [x] 14.4 Crear `test/auth_web_session_test.go` con `package test` y un `fakeWebSessionStore` (campos para capturar el último `Save`/`Delete` y un `findErr error`, un `findSession auth.WebSession`). Añadir: `TestWebSession_CreateStoresAndReturnsOpaqueID` (dos llamadas a `Create(ctx, "admin")` devuelven IDs no vacíos y distintos entre sí, y el fake recibió un `Save` con `Username=="admin"`), `TestWebSession_ValidateUnknownReturnsErr` (con `findErr = auth.ErrWebSessionNotFound`, `Validate(ctx, "abc")` devuelve `auth.ErrWebSessionNotFound` verificado con `errors.Is`; y `Validate(ctx, "")` devuelve `auth.ErrWebSessionNotFound` sin invocar `Find`), `TestWebSession_RevokeDeletes` (`Revoke(ctx, "id1")` invoca `Delete` del fake con `"id1"`; `Revoke(ctx, "")` no invoca `Delete`). Criterio de listo: `go test ./test/... -run TestWebSession` pasa.

### 14c. Adapter Redis de sesión web (pkg/persistence/redis)

- [x] 14.5 Crear `pkg/persistence/redis/web_session_store.go` con `package redis`, imports `"context"`, `"encoding/json"`, `"fmt"`, `"time"`, `goredis "github.com/redis/go-redis/v9"` y `"github.com/HongXiangZuniga/mongo-agent/pkg/auth"`, según `design.md` D12. Definir el DTO `type webSessionDoc struct { Username string \`json:"username"\`; CreatedAt int64 \`json:"created_at"\` }`, el struct `type webSessionStore struct { client *goredis.Client; ttl time.Duration }`, el constructor `func NewWebSessionStore(client *goredis.Client, ttl time.Duration) auth.WebSessionStore { return &webSessionStore{client, ttl} }`, un helper `func (s *webSessionStore) key(id string) string { return fmt.Sprintf("web_session:%s", id) }`, e implementar: `Save` (marshalar `webSessionDoc{Username: session.Username, CreatedAt: session.CreatedAt.Unix()}` a JSON y `s.client.Set(ctx, s.key(sessionID), data, s.ttl)`); `Find` (`s.client.Get(ctx, s.key(sessionID)).Bytes()`; si `err == goredis.Nil` devolver `auth.WebSession{}, auth.ErrWebSessionNotFound`; si otro error, devolverlo; si ok, unmarshalar y devolver `auth.WebSession{Username: doc.Username, CreatedAt: time.Unix(doc.CreatedAt, 0)}`); `Delete` (`s.client.Del(ctx, s.key(sessionID)).Err()`). Criterio de listo: compila; `grep -n 'web_session:' pkg/persistence/redis/web_session_store.go` devuelve 1 línea; `grep -nE 'goredis.Nil' pkg/persistence/redis/web_session_store.go` devuelve al menos 1 línea.
- [x] 14.6 Crear `test/integration/web_session_store_test.go` con el build tag `//go:build integration` (primera línea) y `package integration`. Añadir `TestWebSessionStore_SaveFindDeleteRoundTrip` que se conecta a `REDIS_ADDR` (leído con `os.Getenv`; si está vacío, `t.Skip`), construye `redis.NewWebSessionStore(client, time.Minute)`, hace `Save` de una `auth.WebSession{Username:"admin", CreatedAt: time.Now()}`, verifica que `Find` devuelve el mismo `Username`, luego `Delete` y verifica que `Find` devuelve `auth.ErrWebSessionNotFound` (con `errors.Is`). Criterio de listo: `go test -tags=integration ./test/integration/... -run TestWebSessionStore` pasa con un Redis disponible; `go test ./test/...` (sin el tag) no compila ni ejecuta este archivo.

### 14d. Adapter web: cookie = session ID, validación y logout (pkg/http/web)

- [x] 14.7 En `pkg/http/web/config.go`, eliminar el campo `APIToken string` del struct `CookieConfig` (quedan `CookieName`, `MaxAge`, `Secure`). Criterio de listo: `grep -n 'APIToken' pkg/http/web/config.go` no devuelve ninguna línea.
- [x] 14.8 En `pkg/http/web/middleware.go`, cambiar la firma a `func CookieAuthMiddleware(cfg CookieConfig, sessions auth.WebSessionManager) gin.HandlerFunc` (añadir el import `"github.com/HongXiangZuniga/mongo-agent/pkg/auth"`). Reemplazar el cuerpo: leer `sessionID, err := c.Cookie(cfg.CookieName)`; si `err != nil` o `sessionID == ""`, `denyAuth(c); return`; si `_, err := sessions.Validate(c.Request.Context(), sessionID); err != nil`, `denyAuth(c); return`; si no, `c.Next()`. Eliminar el import `"crypto/subtle"` y toda referencia a `cfg.APIToken`. NO tocar `denyAuth` (comportamiento htmx/normal intacto). Criterio de listo: compila tras 14.10/14.11; `grep -n 'subtle' pkg/http/web/middleware.go` no devuelve nada; `grep -n 'sessions.Validate' pkg/http/web/middleware.go` devuelve 1 línea.
- [x] 14.9 En `pkg/http/web/handlers.go`, según `design.md` D6: (a) añadir al struct `webPort` el campo `webSessions auth.WebSessionManager`; (b) cambiar `NewWebHandler` a `func NewWebHandler(agentService agent.AgentService, cookieCfg CookieConfig, scrubber *utils.SecretScrubber, authenticator auth.Authenticator, webSessions auth.WebSessionManager) WebHandlers` con cuerpo `return &webPort{agentService, cookieCfg, scrubber, authenticator, webSessions}`; (c) en `Login`, tras `Authenticate` correcto, hacer `sessionID, err := p.webSessions.Create(c.Request.Context(), username)`; si `err != nil`, responder `503` re-renderizando `login` con `Error: "No se pudo iniciar la sesión, inténtalo de nuevo"` y `return`; si ok, en el `c.SetCookie` usar `sessionID` como valor (segundo argumento) en lugar de `p.cookieCfg.APIToken`, manteniendo el resto de argumentos (nombre, `MaxAge`, path `/web`, `Secure`, `HttpOnly=true`) y `c.Redirect(http.StatusSeeOther, "/web")`; (d) en `Logout`, antes de expirar la cookie, leer `sessionID, err := c.Cookie(p.cookieCfg.CookieName)`; si `err == nil && sessionID != ""`, `_ = p.webSessions.Revoke(c.Request.Context(), sessionID)`; luego mantener el `SetCookie` de expiración (`MaxAge=-1`) y el `Redirect` a `/web/login` como hoy. Criterio de listo: compila tras 14.11; `grep -n 'p.webSessions.Create' pkg/http/web/handlers.go` devuelve 1 línea; `grep -n 'p.webSessions.Revoke' pkg/http/web/handlers.go` devuelve 1 línea; `grep -n 'APIToken' pkg/http/web/handlers.go` no devuelve ninguna línea.
- [x] 14.10 En `pkg/http/web/router.go`, cambiar la firma a `func RegisterRoutes(r *gin.Engine, h WebHandlers, cfg CookieConfig, sessions auth.WebSessionManager)` (añadir el import `"github.com/HongXiangZuniga/mongo-agent/pkg/auth"`) y aplicar `authed.Use(CookieAuthMiddleware(cfg, sessions))`. El resto de rutas no cambia. Criterio de listo: compila tras 14.11; `grep -n 'CookieAuthMiddleware(cfg, sessions)' pkg/http/web/router.go` devuelve 1 línea.

### 14e. Wiring en el composition root (cmd/server/api.go)

- [x] 14.11 En `cmd/server/api.go`, según `design.md` D7: (a) en `main()`, tras construir `authService`, añadir `webSessionStore := redisadapter.NewWebSessionStore(redisClient, webSessionMaxAge)` y `webSessionManager := auth.NewWebSessionManager(webSessionStore)`; (b) reemplazar los DOS literales `web.CookieConfig{...}` (el de `NewWebHandler` y el de `RegisterRoutes`) por un único `cookieCfg := web.CookieConfig{CookieName: webAuthCookieName, MaxAge: webSessionMaxAge, Secure: webCookieSecure}` (SIN el campo `APIToken`); (c) pasar `webSessionManager` como quinto argumento de `web.NewWebHandler(agentService, cookieCfg, secretScrubber, authService, webSessionManager)`; (d) pasar `webSessionManager` como cuarto argumento de `web.RegisterRoutes(r, webHandler, cookieCfg, webSessionManager)`. `apiToken` se sigue usando en `rest.RegisterRoutes(r, agentHandler, apiToken)` y en `secretScrubber` (no tocar). Criterio de listo: `go build ./...` compila; `grep -n 'NewWebSessionStore' cmd/server/api.go` devuelve 1 línea; `grep -n 'APIToken' cmd/server/api.go` no devuelve ninguna línea; `grep -n 'VerifyReadOnlyGuarantee' cmd/server/api.go` sigue devolviendo 1 línea con argumento `mdb`.

### 14f. Tests del adapter web (pkg/http/web / test)

- [x] 14.12 Añadir/actualizar los tests del handler y middleware web con un `fakeAuthenticator` y un `fakeWebSessionManager` (que capture el `username` recibido en `Create`, devuelva un ID fijo conocido, permita configurar el error de `Validate`, y capture el `sessionID` recibido en `Revoke`). Cubrir: `TestLogin_ValidCredentialsCreatesSessionAndSetsCookie` (cookie fijada con valor == el ID que devuelve el fake, NO `API_TOKEN`; `303` a `/web`), `TestLogin_InvalidCredentialsReturns401` (el `fakeAuthenticator` falla → `401`, sin cookie, y `Create` NO se invoca), `TestCookieAuth_ValidSessionAllows` (cookie con ID que `Validate` acepta → ruta protegida `200`), `TestCookieAuth_InvalidSessionRedirects` (`Validate` falla → `303` a `/web/login`; con header `HX-Request: true` responde `HX-Redirect: /web/login`), `TestCookieAuth_ArbitraryCookieRejected` (regresión: una cookie con valor igual a un `API_TOKEN` de ejemplo, no registrado, es rechazada), `TestLogout_RevokesServerSession` (`POST /web/logout` con cookie de sesión invoca `Revoke` con ese ID y expira la cookie). Si las firmas de tests previos (`TestLogin_*`) quedaron obsoletas por el nuevo constructor, actualizarlas para pasar el `fakeWebSessionManager`. Criterio de listo: `go test ./...` (o `make unit-test`) pasa, incluidos estos tests.

### 14g. Documentación y validación de la corrección

- [x] 14.13 En `README.md`, actualizar la descripción del login web para reflejar la corrección: la interfaz `/web` ahora usa **sesiones de servidor** con un identificador opaco guardado en Redis y validado en cada petición; la cookie ya NO transporta `API_TOKEN`; el logout invalida la sesión del lado servidor; `POST /ask` sigue autenticándose por `API_TOKEN` (sin cambios). Documentar que `WEB_SESSION_MAX_AGE_SECONDS` fija tanto el `MaxAge` de la cookie como el TTL de la sesión web en Redis. Criterio de listo: `grep -ni 'sesión\|session' README.md` muestra la nueva descripción; `grep -n 'WEB_SESSION_MAX_AGE_SECONDS' README.md` devuelve al menos 1 línea.
- [x] 14.14 Ejecutar `gofmt -l .`, `go build ./...` y `make unit-test` (o `go test ./test/...`) desde la raíz y confirmar que no hay archivos sin formatear, compila y pasan todos los tests (incluidos los nuevos de 14.4 y 14.12). Los tests de integración (14.6) quedan fuera de `make unit-test`. Criterio de listo: los tres comandos terminan sin error.
- [x] 14.15 Ejecutar `npx @fission-ai/openspec@latest validate add-user-login --strict` desde la raíz y confirmar que no reporta errores. Criterio de listo: el comando termina sin errores de validación.
