## 1. Utilidades de sanitización (pkg/utils)

- [x] 1.1 Crear el archivo `pkg/utils/secrets.go` con `package utils`. Añadir el comentario de paquete: `// Este archivo contiene utilidades de redacción de secretos. Ninguna función aquí debe registrar, loguear ni devolver el valor crudo de un secreto.`. Criterio de listo: `gofmt -l pkg/utils/secrets.go` no reporta errores de formato.
- [x] 1.2 En `pkg/utils/secrets.go`, definir `type SecretScrubber struct { secrets []string }`. Definir `func NewSecretScrubber(secrets ...string) *SecretScrubber` que filtra y descarta cualquier cadena vacía de `secrets` antes de guardarlas en el struct (para no reemplazar accidentalmente cadenas vacías por `[REDACTED]`). Criterio de listo: `NewSecretScrubber("", "abc", "")` produce un `*SecretScrubber` cuyo campo `secrets` tiene longitud 1 (verificado en el test de la tarea 1.4).
- [x] 1.3 En `pkg/utils/secrets.go`, definir `func (s *SecretScrubber) Scrub(text string) string` que, para cada secreto no vacío en `s.secrets`, reemplaza **todas** las ocurrencias de ese secreto en `text` por la cadena literal `"[REDACTED]"` usando `strings.ReplaceAll`, y devuelve el resultado final tras aplicar todos los reemplazos en orden. Si `s` es `nil`, `Scrub` devuelve `text` sin modificar (no debe hacer panic). Criterio de listo: compila y pasa los tests de la tarea 1.4.
- [x] 1.4 Crear `test/utils_test.go` (añadir al archivo ya existente, no crear uno nuevo) con las funciones `TestSecretScrubber_RedactsConfiguredSecrets` (dado un scrubber construido con `NewSecretScrubber("tok123", "key456")`, `Scrub("error: tok123 failed with key456")` debe ser igual a `"error: [REDACTED] failed with [REDACTED]"`), `TestSecretScrubber_LeavesTextWithoutSecretsUnchanged` (`Scrub("plain error")` devuelve `"plain error"` sin cambios), `TestSecretScrubber_IgnoresEmptySecrets` (`NewSecretScrubber("", "abc")` seguido de `Scrub("abc and empty string stays")` no reemplaza subcadenas vacías de forma no controlada), `TestSecretScrubber_NilScrubberIsNoop` (`(*utils.SecretScrubber)(nil).Scrub("text")` devuelve `"text"` sin panic). Criterio de listo: `go test ./test/... -run TestSecretScrubber` pasa.
- [x] 1.5 En `pkg/utils/secrets.go`, importar `"regexp"` y definir el paquete-nivel `var mongoCredentialsPattern = regexp.MustCompile(`([a-zA-Z][a-zA-Z0-9+.-]*://)([^:/@\s]+):([^@/\s]+)@`)`. Definir `func RedactMongoCredentials(text string) string` que devuelve `mongoCredentialsPattern.ReplaceAllString(text, "${1}[REDACTED]:[REDACTED]@")`. Criterio de listo: compila sin errores.
- [x] 1.6 En `test/utils_test.go`, añadir `TestRedactMongoCredentials_RedactsStandardURI` (input `"failed to connect: mongodb+srv://FAKEUSER:FAKEPASSWORD@example-cluster.test/?retryWrites=false"`, el resultado NO debe contener `"FAKEUSER"` ni `"FAKEPASSWORD"`, y SÍ debe contener `"mongodb+srv://[REDACTED]:[REDACTED]@example-cluster.test"`), `TestRedactMongoCredentials_RedactsGenericSchemeURI` (input con `"redis://FAKEUSER:FAKEPW@host:6379"`, el resultado no contiene `"FAKEUSER:FAKEPW"`), `TestRedactMongoCredentials_LeavesTextWithoutURIUnchanged` (input `"collection not found"` devuelve el mismo texto sin cambios), `TestRedactMongoCredentials_RedactsEvenWithoutExactURIMatch` (input con una URI de formato distinto al ejemplo anterior, ej. `"mongodb://FAKEUSER2:FAKEPASS2@10.0.0.5:27017"`, también se redacta, demostrando que no depende de un valor exacto conocido de antemano). Nota: los fixtures usan credenciales obviamente sintéticas (no un usuario/contraseña con apariencia real) para evitar falsos positivos del escáner de secretos de GitHub sobre el patrón de URI de MongoDB Atlas. Criterio de listo: `go test ./test/... -run TestRedactMongoCredentials` pasa.
- [x] 1.7 En `pkg/utils/validation.go` (archivo ya existente), añadir el import `"unicode"` si no está presente, y definir `func SanitizeUserText(text string) string` que usa `strings.Map` para eliminar cualquier rune donde `unicode.IsControl(r)` sea verdadero, excepto `'\n'` y `'\t'`, que se preservan. Criterio de listo: compila; `gofmt -l pkg/utils/validation.go` no reporta errores.
- [x] 1.8 En `test/utils_test.go`, añadir `TestSanitizeUserText_RemovesControlCharacters` (input `"hola\x00mundo\x1b[31m"`, el resultado no contiene `\x00` ni `\x1b`, y contiene `"holamundo"` como subcadena tras la eliminación), `TestSanitizeUserText_PreservesNewlineAndTab` (input `"linea1\nlinea2\ttab"` se devuelve sin cambios), `TestSanitizeUserText_PreservesUnicodePrintable` (input `"¿Cuántas tarjetas hay? 🎉"` se devuelve sin cambios), `TestSanitizeUserText_AllControlCharsResultsInEmptyString` (input compuesto solo por `"\x00\x01\x02"` produce cadena vacía `""`). Criterio de listo: `go test ./test/... -run TestSanitizeUserText` pasa.

## 2. System prompt endurecido (cmd/server/api.go)

- [x] 2.1 En `cmd/server/api.go`, reemplazar el contenido de la constante `systemPrompt` (líneas 40-43 actuales) por el texto exacto especificado en `design.md`, sección "D1", manteniendo la declaración `const systemPrompt = \`...\`` (backtick raw string, sin ningún `fmt.Sprintf` ni concatenación con variables). El nuevo contenido debe incluir, como mínimo, las cuatro reglas numeradas de seguridad descritas en D1. Criterio de listo: `grep -c 'REGLAS DE SEGURIDAD' cmd/server/api.go` devuelve `1`; `gofmt -l cmd/server/api.go` no reporta errores.
- [x] 2.2 Verificar que la declaración sigue siendo `const systemPrompt` y no `var systemPrompt`. Criterio de listo: `grep -n 'const systemPrompt' cmd/server/api.go` devuelve exactamente 1 línea; `grep -n 'var systemPrompt' cmd/server/api.go` no devuelve ninguna línea.
- [x] 2.3 Verificar que ninguna variable de entorno se interpola dentro del bloque de `systemPrompt`. Criterio de listo: entre la línea de `const systemPrompt = ` y el backtick de cierre, `grep` no encuentra ninguna de las cadenas `os.Getenv`, `mongodbURI`, `redisPassword`, `apiToken`, `opencodeAPIKey`.

## 3. Detección de fuga del system prompt (pkg/agent)

- [x] 3.1 En `pkg/agent/service.go`, añadir la constante de paquete `const systemPromptLeakMinMatchChars = 60` y la constante `const systemPromptLeakRefusalMessage = "No puedo compartir esa información."`. Criterio de listo: compila.
- [x] 3.2 En `pkg/agent/service.go`, añadir la función `func detectSystemPromptLeak(answer, systemPrompt string, minMatchChars int) bool` con la implementación exacta descrita en `design.md` sección "D2" (ventana deslizante de `minMatchChars` runes sobre `systemPrompt`, comparación insensible a mayúsculas/minúsculas mediante `strings.ToLower`, `strings.Contains` sobre `answer` en minúsculas). Si `len([]rune(systemPrompt)) < minMatchChars`, devuelve `false` inmediatamente. Criterio de listo: compila; pasa los tests de la tarea 3.5.
- [x] 3.3 En `pkg/agent/service.go`, método `Ask`, localizar el bloque `if len(resp.Message.ToolCalls) == 0 { return Answer{...}, nil }` (línea ~121-127 actual). Modificarlo para que, **antes** de la llamada a `s.sessions.AppendMessage(askCtx, q.SessionID, resp.Message)` que persiste el mensaje del asistente (línea ~116), se ejecute: si `len(resp.Message.ToolCalls) == 0 && detectSystemPromptLeak(resp.Message.Content, s.systemPrompt, systemPromptLeakMinMatchChars)`, entonces reasignar `resp.Message.Content = systemPromptLeakRefusalMessage` y registrar `log.Printf("[Ask] possible system prompt leak detected, session=%s", q.SessionID)` antes de continuar con la persistencia y el `return` normales. El resto del flujo (persistir `resp.Message` ya modificado, devolver `Answer{... Text: resp.Message.Content ...}`) no cambia de estructura. Añadir el import `"log"` si no está presente. Criterio de listo: compila; pasa los tests de la tarea 3.5.
- [x] 3.4 Verificar que la detección solo se aplica cuando `len(resp.Message.ToolCalls) == 0` (respuesta final), no en mensajes intermedios con tool calls. Criterio de listo: revisar manualmente que el bloque de la tarea 3.3 está dentro del `if` que ya comprueba `len(resp.Message.ToolCalls) == 0`, no fuera de él.
- [x] 3.5 En `test/agent_service_test.go`, añadir `TestDetectSystemPromptLeak_NoMatchBelowThreshold` (con un `systemPrompt` de prueba y un `answer` que comparte menos de 60 caracteres consecutivos, `detectSystemPromptLeak` devuelve `false`), `TestDetectSystemPromptLeak_MatchAtThreshold` (un `answer` que contiene una ventana exacta de 60+ caracteres consecutivos del `systemPrompt` de prueba, `detectSystemPromptLeak` devuelve `true`), `TestDetectSystemPromptLeak_CaseInsensitive` (la misma ventana pero con mayúsculas/minúsculas alteradas en `answer`, sigue devolviendo `true`), `TestAsk_FinalAnswerLeakingSystemPromptIsReplacedWithRefusal` (usando un `fakeLLMClient` que devuelve como respuesta final un `Message.Content` que contiene el `systemPrompt` completo pasado a `NewAgentService`, verificar que `Answer.Text` devuelto por `Ask` es exactamente `"No puedo compartir esa información."` y NO contiene ninguna subcadena del `systemPrompt` original), `TestAsk_FinalAnswerWithoutLeakIsUnchanged` (respuesta final normal sin relación con el `systemPrompt`, `Answer.Text` es igual al contenido devuelto por el LLM fake, sin modificar). Criterio de listo: `go test ./test/... -run 'TestDetectSystemPromptLeak|TestAsk_FinalAnswer'` pasa.

## 4. Sanitización de la pregunta del usuario (pkg/agent)

- [x] 4.1 En `pkg/agent/service.go`, método `Ask`, inmediatamente después de la comprobación `if q.Text == "" { return Answer{}, utils.ErrEmptyQuestion() }` (línea ~67-69 actual), añadir: `q.Text = utils.SanitizeUserText(q.Text)` seguido de `if strings.TrimSpace(q.Text) == "" { return Answer{}, utils.ErrEmptyQuestion() }`. El import `"strings"` ya está presente en el archivo. Criterio de listo: compila; pasa los tests de la tarea 4.2.
- [x] 4.2 En `test/agent_service_test.go`, añadir `TestAsk_SanitizesControlCharactersInQuestionBeforePersisting` (enviar una `Question{Text: "hola\x00mundo"}` a un `service` construido con un `fakeSessionStore`; tras `Ask`, verificar inspeccionando el historial capturado por `fakeSessionStore` que el primer mensaje `RoleUser` persistido tiene `Content` igual a `"holamundo"`, sin el carácter `\x00`), `TestAsk_RejectsQuestionThatIsOnlyControlCharacters` (enviar `Question{Text: "\x00\x01\x02"}`, `Ask` devuelve `utils.ErrEmptyQuestion()` mediante `errors.Is`, y el `fakeSessionStore` no recibe ninguna llamada a `AppendMessage`). Criterio de listo: `go test ./test/... -run TestAsk_Sanitizes` y `go test ./test/... -run TestAsk_RejectsQuestionThatIsOnly` pasan.

## 5. Sanitización de errores de tool calls (pkg/agent/tools.go)

- [x] 5.1 En `pkg/agent/tools.go`, en la función `DispatchToolCall`, localizar el bloque final `if repoErr != nil { return fmt.Sprintf("repository error: %v", repoErr), true }` (líneas ~187-189 actuales). Modificarlo a: `if repoErr != nil { return utils.RedactMongoCredentials(fmt.Sprintf("repository error: %v", repoErr)), true }`. El import `"github.com/HongXiangZuniga/mongo-agent/pkg/utils"` ya está presente en el archivo (se usa en `hasDisallowedStage`/`ErrDisallowedPipelineStage`). Criterio de listo: compila; `grep -n 'utils.RedactMongoCredentials' pkg/agent/tools.go` devuelve al menos 1 línea.
- [x] 5.2 En `test/agent_tools_test.go`, añadir un fake de repositorio (o reutilizar `fakeMongoRepo` si ya expone un modo de error configurable; si no lo expone, añadir un campo `findErr error` a `fakeMongoRepo` en el archivo de fakes compartido y usarlo en el método `Find`) que devuelva un error con el formato `errors.New("server selection error: mongodb+srv://FAKEUSER:FAKEPASSWORD@example-cluster.test: context deadline exceeded")` (credenciales sintéticas, evitando el patrón real de URI de Atlas para no disparar falsos positivos del escáner de secretos de GitHub) al invocar `query_find`. Añadir `TestDispatchToolCall_RedactsMongoCredentialsInRepositoryError` que invoca `DispatchToolCall` con ese fake y verifica que el `result` devuelto NO contiene `"FAKEUSER"` ni `"FAKEPASSWORD"`, y SÍ contiene `"[REDACTED]"`. Criterio de listo: `go test ./test/... -run TestDispatchToolCall_RedactsMongoCredentials` pasa.
- [x] 5.3 Añadir `TestDispatchToolCall_PreservesNonCredentialErrorContent` que invoca `DispatchToolCall` con un fake que devuelve `errors.New("collection not found")` y verifica que el `result` sigue conteniendo la subcadena `"collection not found"` sin alterar (la redacción no debe modificar mensajes de error que no contienen credenciales). Criterio de listo: `go test ./test/... -run TestDispatchToolCall_PreservesNonCredentialErrorContent` pasa.

## 6. Composition root: SecretScrubber, generalización de redacción, warning de Redis (cmd/server/api.go)

- [x] 6.1 En `cmd/server/api.go`, en `init()`, después del bloque de llamadas a `requireEnv` (líneas ~93-96 actuales), añadir: `if redisPassword == "" { log.Println("warning: REDIS_PASSWORD is empty; Redis is running without authentication (acceptable for local development only)") }`. Criterio de listo: compila; `grep -n 'REDIS_PASSWORD is empty' cmd/server/api.go` devuelve 1 línea.
- [x] 6.2 En `cmd/server/api.go`, declarar una variable de paquete `secretScrubber *utils.SecretScrubber` (añadir el import `"github.com/HongXiangZuniga/mongo-agent/pkg/utils"` si no está presente). En `main()`, como primera línea del cuerpo de la función, inicializar `secretScrubber = utils.NewSecretScrubber(apiToken, mongodbURI, opencodeAPIKey, redisPassword)`. Criterio de listo: compila.
- [x] 6.3 En `cmd/server/api.go`, función `redactURI(err error, uri string) error` (líneas ~146-151 actuales), reemplazar su cuerpo para que use el mecanismo genérico en vez de (o además de) el reemplazo exacto: `func redactURI(err error, uri string) error { if err == nil { return err }; msg := utils.RedactMongoCredentials(err.Error()); if uri != "" { msg = strings.ReplaceAll(msg, uri, "<redacted-mongodb-uri>") }; if secretScrubber != nil { msg = secretScrubber.Scrub(msg) }; return errors.New(msg) }`. Criterio de listo: compila; `grep -n 'utils.RedactMongoCredentials' cmd/server/api.go` devuelve al menos 1 línea.
- [x] 6.4 En `cmd/server/api.go`, función `initRedis`, en la línea `log.Fatalf("failed to connect to Redis: %v", err)` (línea ~162 actual), envolver el error con el mismo tratamiento: reemplazar por `log.Fatalf("failed to connect to Redis: %v", redactURI(err, ""))` (reutilizando `redactURI` con `uri=""` para aplicar solo la redacción genérica + el scrubber de secretos, sin reemplazo de una URI de Mongo que no aplica aquí). Criterio de listo: compila; `grep -n 'redactURI(err, "")' cmd/server/api.go` devuelve 1 línea.
- [x] 6.5 Verificar que `secretScrubber` se construye antes de cualquier llamada a `initMongo`/`initRedis` en `main()` (el orden de las líneas en `main()` debe tener la inicialización de `secretScrubber` de la tarea 6.2 antes de `mdb := initMongo(...)`). Criterio de listo: revisión manual del orden de líneas en `main()`.

## 7. Logs saneados en los adapters HTTP (pkg/http/rest, pkg/http/web)

- [x] 7.1 En `pkg/http/rest/agent.go`, modificar `NewAgentHandler` para que reciba un parámetro adicional: `func NewAgentHandler(agentService agent.AgentService, scrubber *utils.SecretScrubber) AgentHandlers`, y el struct `agentPort` para que incluya el campo `scrubber *utils.SecretScrubber`. Actualizar la construcción `&agentPort{agentService}` a `&agentPort{agentService, scrubber}`. Criterio de listo: compila (tras actualizar también el call site en la tarea 7.3).
- [x] 7.2 En `pkg/http/rest/agent.go`, método `AskQuestion`, modificar la línea `log.Printf("[AskQuestion] error: %v", err)` (línea ~77 actual) a `log.Printf("[AskQuestion] error: %v", p.sanitizeLogError(err))`, y añadir el método `func (p *agentPort) sanitizeLogError(err error) string { msg := utils.RedactMongoCredentials(err.Error()); if p.scrubber != nil { msg = p.scrubber.Scrub(msg) }; return msg }`. Criterio de listo: compila; pasa el test de la tarea 7.5.
- [x] 7.3 En `cmd/server/api.go`, actualizar el call site `agentHandler := rest.NewAgentHandler(agentService)` a `agentHandler := rest.NewAgentHandler(agentService, secretScrubber)`. Criterio de listo: compila.
- [x] 7.4 Aplicar el mismo patrón de las tareas 7.1-7.3 a `pkg/http/web/handlers.go`: `NewWebHandler` recibe un parámetro adicional `scrubber *utils.SecretScrubber`, el struct `webPort` incluye el campo, el `log.Printf("[SendMessage] error: %v", err)` (línea ~189 actual) se reemplaza por la misma sanitización, y el call site en `cmd/server/api.go` (`webHandler := web.NewWebHandler(agentService, web.CookieConfig{...})`) se actualiza para pasar `secretScrubber` como argumento adicional. Criterio de listo: compila.
- [x] 7.5 En `test/http_rest_test.go`, añadir `TestAskQuestion_ErrorLogDoesNotContainSecret` que: (a) construye un `fakeAgentService` cuyo método `Ask` devuelve un error que contiene la cadena `"supersecrettoken123"` embebida en el mensaje (ej. `errors.New("llm provider unavailable: request failed with header Authorization: Bearer supersecrettoken123")`), (b) construye `rest.NewAgentHandler(fakeService, utils.NewSecretScrubber("supersecrettoken123"))`, (c) redirige la salida de `log` a un `bytes.Buffer` con `log.SetOutput(&buf)` antes de la petición y restaura `log.SetOutput(os.Stderr)` con `defer` al final del test, (d) ejecuta `POST /ask` con un token válido, (e) verifica que `buf.String()` NO contiene `"supersecrettoken123"` y SÍ contiene `"[REDACTED]"`. Criterio de listo: `go test ./test/... -run TestAskQuestion_ErrorLogDoesNotContainSecret` pasa.

## 8. Verificaciones estáticas de aislamiento estructural

- [x] 8.1 Ejecutar `grep -rn '"os"' pkg/agent/*.go | grep -v _test.go` y confirmar que no devuelve ninguna línea. Si devuelve alguna, es un defecto a corregir antes de continuar (ningún archivo de producción de `pkg/agent` debe importar `os`). Criterio de listo: el comando anterior no produce salida.
- [x] 8.2 Ejecutar `grep -n 'mongodbURI\|redisPassword\|apiToken\|opencodeAPIKey' pkg/agent/*.go | grep -v _test.go` y confirmar que no devuelve ninguna línea (ninguna función de `pkg/agent` referencia estos nombres de variable, que solo existen en `cmd/server`). Criterio de listo: el comando anterior no produce salida.

## 9. Documentación (README.md)

- [x] 9.1 En `README.md`, dentro de la sección `## Seguridad` existente, añadir una subsección `### Manejo de secretos` inmediatamente después de la lista de bullets existente, con el siguiente contenido mínimo: una tabla o lista que enumere `API_TOKEN`, `MONGODB_URI`, `MONGODB_DB_NAME`, `OPENCODE_API_KEY` como variables que provocan que el proceso no arranque si faltan (fail-fast), y `REDIS_PASSWORD` como variable opcional que genera una advertencia en logs si está vacía pero no impide el arranque; una frase explícita indicando que ningún secreto se registra en logs, completo ni parcialmente, y que los mensajes de error expuestos al cliente HTTP y al LLM son genéricos o están saneados de credenciales de MongoDB. Criterio de listo: `grep -n '### Manejo de secretos' README.md` devuelve 1 línea.
- [x] 9.2 En `README.md`, dentro de la sección `## Seguridad`, añadir un bullet adicional describiendo la defensa contra prompt injection: el system prompt prohíbe revelar su propio contenido y configuración interna, trata el contenido de las tools como datos no confiables, y existe una verificación de salida que sustituye la respuesta por un mensaje genérico si detecta una fuga verbatim del system prompt. Criterio de listo: `grep -n 'prompt injection' README.md` devuelve al menos 1 línea (insensible a mayúsculas si se usa una variante de capitalización distinta, ajustar el grep con `-i`).
- [x] 9.3 Verificar que `.env.example` no contiene ningún valor real en `API_TOKEN`, `MONGODB_URI`, `OPENCODE_API_KEY`, `REDIS_PASSWORD` (deben seguir siendo placeholders entre `<...>` o cadenas vacías, como ya es el caso). Criterio de listo: `grep -E '^(API_TOKEN|OPENCODE_API_KEY)=' .env.example` solo muestra valores con el patrón `<...>`, y `grep -E '^MONGODB_URI=' .env.example` muestra una URI de ejemplo con placeholders `<db_username>`/`<db_password>`/`<your-cluster>`, no credenciales reales.

## 10. Validación final

- [x] 10.1 Ejecutar `gofmt -l .` desde la raíz del repositorio y confirmar que no reporta ningún archivo con problemas de formato. Si reporta alguno, ejecutar `gofmt -w <archivo>` y volver a verificar.
- [x] 10.2 Ejecutar `go build ./...` desde la raíz del repositorio y confirmar que compila sin errores.
- [x] 10.3 Ejecutar `make unit-test` (o `go test ./test/... -short` si el target `unit-test` no está disponible) y confirmar que todos los tests nuevos y existentes pasan, incluyendo explícitamente los añadidos en las tareas 1.4, 1.6, 1.8, 3.5, 4.2, 5.2, 5.3 y 7.5.
- [x] 10.4 Ejecutar `npx @fission-ai/openspec@latest validate harden-agent-security --strict` desde la raíz del repositorio y confirmar que no reporta errores. Corregir cualquier error de formato en `proposal.md`, `design.md` o `specs/agent-security-hardening/spec.md` antes de dar por completado este change.
